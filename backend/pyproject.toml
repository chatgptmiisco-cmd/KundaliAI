[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
# One event loop for the whole session so the module-level async SQLAlchemy
# engine (app.db.base.engine) isn't torn between event loops across tests.
asyncio_default_fixture_loop_scope = "session"
testpaths = ["tests"]
